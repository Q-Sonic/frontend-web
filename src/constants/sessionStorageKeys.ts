/** Set after successful registration; cleared when the login success banner is dismissed. */
export const SESSION_KEY_POST_REGISTER_LOGIN = 'stagego_postRegisterLoginSuccess';

/** Last registration identification when mode is `local` (no backend field yet). */
export const SESSION_KEY_REGISTRATION_IDENTIFICATION = 'stagego_registrationIdentification';
